# 提交记录：上线治理

- feat(release-governance): 上线治理闭环——release_items 表（上线配置 config / 上线 SQL sql / 上线检查 check + 回滚 + 状态 + 必做）按名 upsert 幂等、CRUD/筛选/排序、随节点级联删除；buildReleaseChecklist 按 self/subtree 聚合完成度/按类型分布/就绪结论/阻塞项（无必做项 ready=null）；编排层 runReleaseChecks（上线清单 + code_check/biz_check/release_check 用例拼提示词派单 + 自动开 running 报告）+ composeReleaseCheckPrompt + renderReleaseChecklistMd；三入口（HTTP/CLI/MCP）1:1；补 18 条单测 + 4 条 HTTP 集成测试 + 老库迁移断言；顺修 TEST_CASE_NAME_EXISTS 漏登记 STATUS_BY_CODE 导致重名用例返回 500 的既有缺陷
- fix(release-governance): 按独立测试复核修复——① checklist 的 done 与 skipped 分开计数，Markdown 摘要改「完成/跳过」，避免「必做项全 skipped」显示成「已完成：0」的误读 ② upsert 已存在时只更新显式传入字段，不再静默清掉 rollback/status/required，并补文档说明覆盖 vs 保留语义 ③ runReleaseChecks 支持 scope=self|subtree（与 release_checklist 口径对齐），self 下若子树有上线项则报错时明确提示改用 scope=subtree ④ 补边界 UT：字段保留/新建默认值/名称 trim 唯一性/大小写口径/subtree 联动
- fix(release-governance): 三入口对齐 release item upsert 的显式字段语义——CLI（server/cli.mjs）与 MCP（server/mcp.mjs）此前在参数装配处补默认值（kind||'config' / rollback??null / status||'pending' / required===undefined?1），使 store 永远看到「显式传入」，重复 upsert 只传部分字段时既有 rollback/status/required 被静默回退；现改为只透传显式字段，默认值职责收敛到 store.createReleaseItem 形参默认值；新增 test/release-upsert-consistency.test.mjs 7 条覆盖 store/HTTP/CLI（真实子进程）/MCP（in-memory 协议）一致性（CLI/MCP 两条在旧实现上失败，已用 5c38748 验证）；design.md 增 R4.3 明确「补默认值是 store 的职责，入口不得补」；npm test 222/222
- fix(release-governance): `release-checklist` / `release-checks` 的 `scope` 收口为枚举校验（D2 连带修复）——此前非法值静默降级 `self`，会让「子树有未完成必做上线项」被判成就绪；现由 `store.normalizeScope` 统一校验（非法 → 400 `VALIDATION_FAILED`），三入口透传原始值；test/http.test.mjs / test/scope-validation.test.mjs 补非法 scope 断言
