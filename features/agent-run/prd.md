# agent 测试运行（测试节点）

## 需求

- 每个子任务支持「测试」：写提示词 → 触发 agent 在节点关联仓库目录中执行
- 目前先支持 qodercli（非交互 `-p`），模型 DeepSeek-Flash（可传参覆盖）
- 运行输出/状态落库可查；默认超时 10 分钟；服务重启自动清理残留运行
- 消费方：IDEA 插件（Review 顶栏「🧪 测试」→ 提示词 + 运行 + 实时输出轮询）；三入口 1:1

## 非目标（当前）

- 其他 agent 适配（claude / codex 等）；多节点批量测试；测试结果与 commit 审查联动
