# 提交记录：需求思维导图

- feat(requirement-mindmap): 需求思维导图——补齐主链路「思维导图」断点：CLI 帮助已列 mindmap 但无 dispatch case（执行即「未知命令」），store/HTTP/render 半成品散落无分支工作区且无 MCP / UT / 文档。buildMindmap 把节点树投影成 mermaid mindmap（缩进表达层级、顶层唯一）+ 结构化 nodes/edges/totals；标签转义双引号与 &（先 & 避免二次转义），空名用占位符，避免空标签解析失败；maxDepth=0..50 深度截断 + totals.truncated 计数，空串/越界/非整数一律 VALIDATION_FAILED（Number('')=0 会静默截成只剩根）；scope/format 复用 normalizeScope/normalizeFormat 不静默降级；纯读不落表不动 revision（与 readiness/delivery_gate 同原则）；三入口 1:1（GET /api/nodes/:id/mindmap · CLI mindmap 单层命令 · MCP mindmap）+ renderMindmapMd + NodeDrawer「导图」页签（默认 subtree，Vditor 自托管 mermaid 渲染）；补 10 条单测 + 7 条三入口集成（含原断点回归、对外契约字段受控、三入口 mermaid 文本一致性）；新增 features/requirement-mindmap 三件套并同步 04-api / api.md / 06-ui / 08-testing / 09-decisions(36,37) / features/README
