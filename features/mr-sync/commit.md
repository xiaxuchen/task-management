# MR 自动拉取功能提交记录

> 状态：**未实现**（计划 5）。
> 已完成的部分：`settings-ui` 的 GitLab 连通性测试（`POST /api/config/gitlab/test`）。

- docs(mr-sync): 建立功能文档 —— prd（需求 + 接口表 + 验收标准）、design（拉取 / upsert / token 安全 / 错误码细分）

（待实现后按约定逐行追加 commit message）