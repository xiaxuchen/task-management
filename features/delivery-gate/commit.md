# 交付门禁 commit 记录

- feat(delivery-gate): 交付门禁（需求就绪 / 测试验收 / 上线治理的最终汇总）——新增只读聚合 `buildDeliveryGate`，把三段既有结论收敛为唯一“能否交付”判定；每个来源三态 `pass/fail/not_applicable`，最终 `ready/not_ready/unknown`（全不适用时 `ready=null`，不伪造成绿灯）；`acceptance` 的 `running`/`notRun` 也视为未取得交付证据；blockers 展平到条目级；三入口 1:1（HTTP `/api/nodes/:id/delivery-gate`、CLI `delivery gate`、MCP `delivery_gate`）+ `renderDeliveryGateMd` + NodeDrawer「交付」页签；纯读、不落表、不动 revision
- fix(delivery-gate): `scope` 收口为枚举校验（D2 连带修复）——`buildDeliveryGate` 改走 `store.normalizeScope`，非法值不再静默降级 `self`；同时其内部 `buildRequirementReadiness` 在「子树无需求」时返回空态而非抛错，交付门禁仍按 `not_applicable` 处理（不伪造成绿灯）
- fix(delivery-gate): 收口独立测试复核 D1/D2/D3——① `scope`/`format` 统一由 `store.normalizeScope` / `store.normalizeFormat` 校验，非法值一律 `VALIDATION_FAILED`；MCP 聚合工具改为 handler 内业务校验并以 `isError` 返回，不再泄漏 SDK `-32602`，HTTP / CLI / MCP 拒绝行为对齐 ② `buildAcceptanceReport` 仅聚合 `enabled=1` 用例，停用用例不再以 `notRun` 阻塞交付，与 readiness 口径一致 ③ `renderDeliveryGateMd` 转义表格单元格 `|`/换行；补 MCP 真实协议测试、四入口逐字段一致性、`not_run`/`running` 桶与 blocker 明细、非法 scope/format 三入口回归
- fix(delivery-gate): 复验残留收口——① `node_diffs` / `node_tracks` / `commit_duplicates` / `release_check` 四个 MCP 工具纳入 `mcpValidate` + `z.string()`，非法 `scope` 不再泄漏 `-32602`，与文档的 `isError + VALIDATION_FAILED` 承诺对齐（横切回归覆盖全部吃 scope 的 MCP 工具）② `renderDeliveryGateMd.cell()` 先转义反斜杠再转义竖线，补「输入本身含反斜杠」边界用例；R2 集成基线确认为 `main`(`4e79e04`)，`agent/xpx-110-workflow-map` 需基于该基线 rebase/合并后再交付
