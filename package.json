{
  "name": "task-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test",
    "test:watch": "node --test --watch",
    "smoke": "node --test --test-name-pattern='smoke'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.2.1"
  }
}
